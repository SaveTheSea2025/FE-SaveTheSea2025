export interface RankItem {
  rank: number;
  name: string;
  totalWeight: number;
  activityCount: number;
}

export interface MonthlyAwardItem {
  title: string;
  name: string;
  value: number;
  unit: string;
}
